import { index, integer, sqliteTable, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const settings = sqliteTable('settings', { id: integer('id').primaryKey(), pinHash: text('pin_hash').notNull() });
export const budgetMonths = sqliteTable('budget_months', { month: text('month').primaryKey(), finalized: integer('finalized').notNull().default(0), finalizedAt: text('finalized_at') });
export const people = sqliteTable('people', { id: text('id').primaryKey(), name: text('name').notNull(), position: integer('position').notNull() });
export const categories = sqliteTable('categories', { id: text('id').primaryKey(), name: text('name').notNull(), monthlyLimitCents: integer('monthly_limit_cents').notNull(), color: text('color').notNull(), position: integer('position').notNull() });
export const categoryMonthlyLimits = sqliteTable('category_monthly_limits', { id: text('id').primaryKey(), categoryId: text('category_id').notNull().references(() => categories.id, { onDelete: 'cascade' }), month: text('month').notNull(), limitCents: integer('limit_cents').notNull() }, table => [index('idx_category_limits_month').on(table.month), uniqueIndex('idx_category_limits_category_month').on(table.categoryId, table.month)]);
export const incomes = sqliteTable('incomes', { id: text('id').primaryKey(), personId: text('person_id').notNull().references(() => people.id, { onDelete: 'cascade' }), amountCents: integer('amount_cents').notNull(), source: text('source').notNull(), date: text('date').notNull() }, table => [index('idx_incomes_date').on(table.date)]);
export const expenses = sqliteTable('expenses', { id: text('id').primaryKey(), personId: text('person_id').notNull().references(() => people.id, { onDelete: 'cascade' }), categoryId: text('category_id').notNull().references(() => categories.id, { onDelete: 'restrict' }), amountCents: integer('amount_cents').notNull(), note: text('note').notNull(), date: text('date').notNull() }, table => [index('idx_expenses_date').on(table.date), index('idx_expenses_category_id').on(table.categoryId)]);
